<?xml version="1.0" encoding="UTF-8"?>
<Файл НаимПрог="AlcoGo 3" ВерсФорм="4.20" ДатаДок="{{ .CurrentDate }}">
    <Документ>
    {{- range $vcode, $vc := .Maps }}
        <Оборот П000000000003="{{$vcode}}" ПN="{{$vc.Pn}}">
    {{- range $r, $regs := .Maps }}
            <СведПроизвИмпорт NameOrg="{{$regs.Short}}" INN="{{$regs.Inn}}" KPP="{{$regs.Kpp}}">
    {{- range $val := .Rows }}
                <Поставка П000000000013="{{$val.DocDate}}" П000000000014="{{$val.DocNumber}}" П000000000015="{{$val.Gtd}}" П000000000016="{{$val.Vol}}" />
    {{- end}}
            </СведПроизвИмпорт>
    {{- end}}
        </Оборот>
    {{- end}}
    </Документ>
</Файл>